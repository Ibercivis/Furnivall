<!DOCTYPE html PUBLIC "-//W3C//DTD HTML 3.2 Final//EN">
<html lang="en">
    <head>
        <title> Furnivall Administration Interface </title>
        <meta name="generator" value="Furnivall" />
    </head>
    <body>
        <h1> Jobs listing </h1>
        <div class="job">
            <p>{{ escape(jobs.__str__()) }}</p>
            {% for ajob in jobs %}
                {% for job in ajob.keys() %} 
                    <h2>Job {{escape(ajob[job].__str__())}}</h2>
                    <form target="#" onclick="$('#form.submit')" id="form">
                        <label for="jobname">Job name </label /> <input type="text" name="jobname" value="{{ajob[job].name}}" /> <br/>
                        <input type="hidden" name="view_name" value="{{job}}" />
                        <input type="hidden" name="job_id" value="" />
                        <input type="submit" /> 
                    </form>
                    <h3> Workunits </h3>
                    {% for work in ajob[job].workunits %}
                        <div class="Workunit">
                            <p> {{ escape(work.__str__()) }} </p>
                        </div>
                    {%end%}
                {%end%}
            {%end%}
        </div>
        <h1> Researcher listing </h1>
        <div class="researchers">
            {{ researchers }}
            {% for researcher in researchers %}
                <label for "researcher_name"> Name: </label> <input type="text" name="researcher_name" value="{{ escape(researcher.user) }}" />
                <label for "researcher_id"> ID: </label> <span name="researcher_id" value="{{ escape(researcher.id) }}" />
                <label for "researcher_name"> Name: </label> <input type="text" name="researcher_name" value="{{ escape(researcher.user) }}" />
                <div class="jobs_creator">    
                    Create new job for a specific researcher.
                    <input type="select"> {% for option in researcher.initialize_views %} <option name="{{option}}"> {{option}} </option> {%end%} </input>
                    <input type="submit" name="Create">
               </div>
            {% end %}
        </div>
    </body>
</html>

