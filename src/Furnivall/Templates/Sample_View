<!DOCTYPE html PUBLIC "-//W3C//DTD HTML 3.2 Final//EN">
<html lang="en">
    <head>
        <title> Furnivall Administration Interface </title>
        <meta name="generator" value="Furnivall" />
    </head>
    <body>
        <h1> Jobs listing </h1>
        <div class="job">
            {% if self.get_argument("create") == 1 %}
            <p>{{ escape(jobs.__str__()) }}</p>
            {% for ajob in jobs %}
                {% for job in ajob.keys() %} 
                    {% if job == slug %}
                    <h2>Hey! I'm job {{ job }} made explicitly for u =)</h2>
                    <form action="#"> <input type="submit" value="Create new job for this view" /> <input type="hidden" name="create" value=1>  </form>
                {%end%}
            {%end%}
        </div>
    </body>
</html>

